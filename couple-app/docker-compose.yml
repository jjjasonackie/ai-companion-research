version: '3.9'

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - PAIR_CODE=LOVEBIRDS
    restart: unless-stopped

volumes: {}
